# Requires Python >= 3.12 (see pyproject.toml) — random.binomialvariate.
#
# Upper bounds matter here: this codebase implements every estimator itself on
# the stdlib, so a surprise major bump in pydantic or openai changes validation
# or client semantics underneath numbers the product claims are defensible.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
openai>=1.50,<2.0
pydantic>=2.9,<3.0

# PDF text extraction for decks and documents pasted as a link. Pure Python by
# requirement, not by accident: this image has no native dependencies, and the
# one thing worse than not reading PDFs is running a C parser over a file an
# arbitrary URL handed us. Page count and byte size are both capped upstream.
pypdf>=5.0,<7.0

# dev / test
pytest>=8.0,<9.0
httpx>=0.27,<1.0
