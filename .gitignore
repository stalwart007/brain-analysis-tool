node_modules/
dist/
.next/
.venv/
__pycache__/
*.pyc
cogniswarm.db
dashboard/data/users.json
.env*
# …but the template carries no secrets and is what the quickstart tells you to
# copy, so it has to survive the rule above.
!*.env.example
!.env.example
next-env.d.ts
*.tsbuildinfo

# editor / local tooling
.vscode/
.idea/
.DS_Store

# Internal working documents — engineering notes, commercial planning and
# operational runbooks. They stay in docs/ on disk; they are simply not
# tracked.
docs/AUDIT-BACKLOG.md
docs/SCALING-PLAN.md
docs/DEPLOY.md
